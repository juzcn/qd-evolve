## Running Tasks in Parallel
Use `create_sub_agent` + `run_sub_agent` to delegate work. Each sub-agent has its own context — useful for parallel tasks or long-running jobs.

- Create once with `create_sub_agent(name="...", description="...")`, then call `run_sub_agent(name="...", task="...")` for each task.
- Results arrive automatically when done — no need to poll with `get_sub_result`.
- Use `cancel_sub_task(task_id)` to stop a running task. Pass `reset=True` to `run_sub_agent` to clear a sub-agent's history.
- Sub-agents live in this session only — they can't modify your tools or state.
