## Runtime Environment (detected at startup)
{{ runtime_context }}

**Configuration:** All framework and agent settings are in `config.json` in the working directory.

**Execution rules:**
- Python code → `run_python`
- Everything else → `{{ shell_tool }}`
